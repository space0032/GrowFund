spring:
  application:
    name: seedtowealth-backend
  
  datasource:
    url: jdbc:mysql://localhost:3306/growfund?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  
  # Internationalization support
  messages:
    basename: i18n/messages
    encoding: UTF-8
    fallback-to-system-locale: false

server:
  port: 8080
  servlet:
    context-path: /api

# Firebase configuration
firebase:
  config-path: ${FIREBASE_CONFIG_PATH:firebase-config.json}

# Application-specific configuration
app:
  # Supported languages for farmers
  supported-languages:
    - en
    - hi
    - ta
    - te
    - mr
    - bn
    - gu
  
  # Game configuration
  game:
    initial-coins: 10000
    max-level: 50
    daily-reward: 100
